
use image::{DynamicImage, GenericImageView, Luma};
use image::GrayImage;
use rayon::prelude::*;

fn fit_quads(img: &dyn GenericImageView<Pixel=Luma<u8>>) {
}


#[cfg(test)]
mod tests {
	use super::*;

	#[test]
	fn test_asdf() {

	}
}